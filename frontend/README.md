# Frontend (React, JavaScript)

Minimal React frontend scaffold using Vite.

Setup
```bash
cd frontend
npm install
npm run dev
```

Files
- `src/main.js` — app entry
- `src/App.js` — root React component
